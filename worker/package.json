{
  "name": "englishword-handwriting-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:openai": "wrangler secret put OPENAI_API_KEY",
    "secret:token": "wrangler secret put ACCESS_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
